# Migration `20200722115859-add-location--post--sentiment--user`

This migration has been generated by Aaron Buxbaum at 7/22/2020, 11:58:59 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Location" (
"id" SERIAL,"latitude" Decimal(65,30)  NOT NULL ,"longitude" Decimal(65,30)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Post" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"url" text  NOT NULL ,"userId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Sentiment" (
"id" SERIAL,"negative" boolean   ,"positive" boolean   ,
    PRIMARY KEY ("id"))

ALTER TABLE "public"."User" DROP COLUMN "name",
ADD COLUMN "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
ALTER COLUMN "age" DROP NOT NULL;

ALTER TABLE "public"."Post" ADD FOREIGN KEY ("id")REFERENCES "public"."Sentiment"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Post" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."User" ADD FOREIGN KEY ("id")REFERENCES "public"."Location"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200721154234-testing-a-migration..20200722115859-add-location--post--sentiment--user
--- datamodel.dml
+++ datamodel.dml
@@ -1,15 +1,40 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
+model Location {
+  id        Int    @default(autoincrement()) @id
+  latitude  Float
+  longitude Float
+  User      User[]
+}
+
+model Post {
+  id        Int       @default(autoincrement()) @id
+  createdAt DateTime  @default(now())
+  url       String
+  sentiment Sentiment @relation(fields: [id], references: [id])
+  User      User      @relation(fields: [userId], references: [id])
+  userId    Int
+}
+
+model Sentiment {
+  id       Int      @default(autoincrement()) @id
+  positive Boolean?
+  negative Boolean?
+  Post     Post[]
+}
+
 model User {
-  id    Int     @default(autoincrement()) @id
-  email String  @unique
-  name  String?
-  age   Int
+  id        Int      @default(autoincrement()) @id
+  email     String   @unique
+  age       Int?
+  createdAt DateTime @default(now())
+  posts     Post[]
+  location  Location @relation(fields: [id], references: [id])
 }
```


