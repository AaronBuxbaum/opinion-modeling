# Migration `20200722144514-first-commit`

This migration has been generated by Aaron Buxbaum at 7/22/2020, 2:45:14 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Location" (
"id" text  NOT NULL ,"latitude" Decimal(65,30)  NOT NULL ,"longitude" Decimal(65,30)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Post" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" text  NOT NULL ,"url" text  NOT NULL ,"userId" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Sentiment" (
"id" text  NOT NULL ,"negative" boolean   ,"positive" boolean   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."User" (
"age" integer   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"email" text  NOT NULL ,"id" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

ALTER TABLE "public"."Post" ADD FOREIGN KEY ("id")REFERENCES "public"."Sentiment"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Post" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."User" ADD FOREIGN KEY ("id")REFERENCES "public"."Location"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200722144514-first-commit
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,40 @@
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Location {
+  id        String @default(uuid()) @id
+  latitude  Float
+  longitude Float
+  User      User[]
+}
+
+model Post {
+  id        String    @default(uuid()) @id
+  createdAt DateTime  @default(now())
+  url       String
+  sentiment Sentiment @relation(fields: [id], references: [id])
+  User      User      @relation(fields: [userId], references: [id])
+  userId    String
+}
+
+model Sentiment {
+  id       String   @default(uuid()) @id
+  positive Boolean?
+  negative Boolean?
+  Post     Post[]
+}
+
+model User {
+  id        String   @default(uuid()) @id
+  email     String   @unique
+  age       Int?
+  createdAt DateTime @default(now())
+  posts     Post[]
+  location  Location @relation(fields: [id], references: [id])
+}
```


